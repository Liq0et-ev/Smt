# --- Snowflake ---
snowflake-connector-python[pandas]==3.12.3

# --- Transformation (Silver/Gold layer) ---
dbt-snowflake==1.9.1

# --- Data ---
pandas==2.2.3
requests==2.32.3
python-dotenv==1.0.1
pycountry==24.6.1

# --- NoSQL (Task 3) ---
pymongo==4.10.1

# --- API (Task 4) ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Dashboard (Task 5) ---
dash==2.18.2
plotly==5.24.1

# --- Forecasting (Task 6) ---
statsmodels==0.14.4
/*
Optional and heavier dependencies (such as visual EDA reporting tools) are
kept separately in requirements-optional.txt.

They are not included in the main dependencies because packages like
ydata-profiling and its dependency chain (numba/llvmlite) may not yet support
the newest Python versions (for example, Python 3.14 at the time of this
commit).

The core project components, including Snowflake, dbt, ETL, API, and dashboard,
do not require these optional packages.
*/
