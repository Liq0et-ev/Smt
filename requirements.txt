# --- Snowflake ---
snowflake-connector-python[pandas]==3.12.3

# --- Transformation (Silver/Gold layer) ---
dbt-snowflake==1.9.1

# --- Data ---
pandas==2.2.3
requests==2.32.3
python-dotenv==1.0.1
pycountry==24.6.1

# --- NoSQL (Task 3) ---
pymongo==4.10.1

# --- API (Task 4) ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Optional, heavier dependencies (visual EDA reports, etc.) live in
# requirements-optional.txt -- kept separate because ydata-profiling's
# dependency chain (numba/llvmlite) tends to lag support for brand-new
# Python versions (e.g. not yet compatible with 3.14 as of this commit).
# The core project (Snowflake, dbt, ETL, API, dashboard) does not need it.
